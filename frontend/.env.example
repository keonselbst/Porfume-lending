# Нужен только для локальной разработки, если бэкенд крутится отдельно
# (например, uvicorn на localhost:8000). На Vercel не задавайте эту
# переменную — фронт и API будут на одном домене, api.js сам подставит
# относительный путь /api.
REACT_APP_BACKEND_URL=http://localhost:8000
